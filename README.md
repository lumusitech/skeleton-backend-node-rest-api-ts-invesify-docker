# skeleton node ts inversify docker docker-compose

after clone, run
```bash
yarn
```
or
```bash
npm install
```
then,
```bash
docker-compose up -d
```
extra: you can use lazydocker terminal app
```bash
lazydocker
```